# Core dependencies with version bounds for binary stability
pandas>=2.0.0,<3.0
dbfread>=2.0.7,<3.0
sqlalchemy>=2.0.0,<3.0
loguru>=0.7.0,<1.0

# SQL Server drivers (at least one required for runtime)
# pyodbc is recommended for Windows systems
pyodbc>=5.0.0,<6.0
pymssql>=2.3.0,<3.0